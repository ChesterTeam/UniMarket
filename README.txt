# 🏪 Campus Marketplace (Локальная версия)

[![FastAPI](https://img.shields.io/badge/FastAPI-005571?style=for-the-badge&logo=fastapi)](https://fastapi.tiangolo.com/)
[![SQLite](https://img.shields.io/badge/SQLite-07405E?style=for-the-badge&logo=sqlite&logoColor=white)](https://sqlite.org)
[![Python](https://img.shields.io/badge/Python-3.10+-3776AB?style=for-the-badge&logo=python&logoColor=white)](https://www.python.org)
[![Windows](https://img.shields.io/badge/Windows-0078D6?style=for-the-badge&logo=windows&logoColor=white)](https://www.microsoft.com/windows)

**Локальный маркетплейс для кампуса** — это полнофункциональное веб-приложение, позволяющее студентам покупать и продавать товары (учебники, электронику, одежду и т.д.) внутри университета. Данная версия работает на вашем компьютере с использованием FastAPI и SQLite.

---

## ✨ Особенности

*   🚀 **Полностью локальная работа**: Не требует интернета после установки. База данных SQLite создается автоматически.
*   📦 **Готовый фронтенд**: Включает адаптированный интерфейс с панелью администратора.
*   ⚡ **Современный бэкенд**: Построен на высокопроизводительном **FastAPI** с автоматической генерацией документации Swagger.
*   🎯 **Простой запуск**: Автоматический скрипт для Windows (`run_all.bat`) настраивает окружение и запускает сервер.
*   🔌 **Умный API-адаптер**: Фронтенд автоматически перенаправляет запросы с локального хранилища на ваш FastAPI-бэкенд.

---

## 🚀 Быстрый старт (Windows)

1.  **Убедитесь, что у вас установлен Python 3.10 или новее.**
    *   Проверить: `python --version` в командной строке.
2.  **Скачайте или клонируйте этот репозиторий.**
3.  **Запустите файл `run_all.bat` двойным кликом.**
    *   Скрипт автоматически создаст виртуальное окружение (`.venv`).
    *   Установит все необходимые зависимости из `requirements.txt`.
    *   Запустит сервер Uvicorn на `http://127.0.0.1:8000`.
4.  **Откройте браузер и перейдите по адресу: [http://127.0.0.1:8000](http://127.0.0.1:8000)**

**Готово!** Приложение запущено. Frontend обслуживается самим FastAPI.

---

## 📁 Структура проекта
.
├── app/ # Backend (FastAPI)
│ ├── main.py # Точка входа FastAPI (API + раздача статики)
│ ├── database.py # Настройка подключения к SQLite
│ ├── models.py # SQLAlchemy ORM модели (таблицы БД)
│ ├── schemas.py # Pydantic-схемы для валидации и сериализации
│ └── crud.py # Бизнес-логика (Create, Read, Update, Delete)
├── campus-marketplace/ # Frontend (статичные файлы)
│ ├── css/
│ ├── img/
│ ├── js/
│ │ └── api_adapter.js # Адаптер для перенаправления вызовов в API
│ ├── pages/
│ └── index.html
├── requirements.txt # Список зависимостей Python
├── run_all.bat # Скрипт автозапуска для Windows
├── campus_marketplace.db # Файл базы данных (создается автоматически)
└── README.md # Эта документация

---

## 🔌 API и эндпоинты

Backend предоставляет REST API с префиксом `/api`. Полную интерактивную документацию (Swagger UI) можно посмотреть после запуска сервера по адресу: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs).

### Основные эндпоинты:

| Метод | Эндпоинт | Описание |
| :--- | :--- | :--- |
| `GET` | `/api/users` | Получить список всех пользователей |
| `GET` | `/api/listings` | Получить список объявлений с фильтрацией |
| `POST` | `/api/listings` | Создать новое объявление |
| `PUT` | `/api/listings/{id}` | Обновить объявление по ID |
| `DELETE` | `/api/listings/{id}` | Удалить объявление по ID |

### Примеры запросов:

**Поиск объявлений с фильтрами:**
```http
GET /api/listings?q=алгебра&category=Учебники&page=1&page_size=12&sort=price_asc

⚠️ Примечания
Изображения в этой версии передаются и хранятся в базе данных в формате base64 (в поле TEXT) или как массив строк.

Некоторые функции фронтенда, которые напрямую зависели от специфики локального хранилища, могут потребовать дополнительной доработки адаптера.

Для продакшн-использования рекомендуется заменить SQLite на PostgreSQL и настроить правильный хостинг для статических файлов и медиа (например, изображений).
